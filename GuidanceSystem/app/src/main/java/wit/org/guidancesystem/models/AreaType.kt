package wit.org.guidancesystem.models

/**
 * Description of space in a building
 * Each Metre object must have an AreaType associated with it
 */
enum class AreaType {
    ROOM, DOOR, CORRIDOR, WALL, DANGER
}